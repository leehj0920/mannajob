<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mannajob.mapper.JoinMapper">
	<!-- 현직자 검색 -->
	<select id="searchEmpl">
		SELECT m.m_id, m.m_name, e.e_corp, e.e_task 
		FROM member m, empl e 
		WHERE m.m_id = e.m_id AND m.m_id = '%'#{m_id}'%' AND m.m_name = '%'#{m_name}'%' AND e.e_corp = '%'#{e_corp}'%' AND e.e_task = '%'#{e_task}'%'  
	</select>
</mapper>
